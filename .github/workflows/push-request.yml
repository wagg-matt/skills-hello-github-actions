name: On push request

on:
  push:
    jobs:
      hello-world:
      runs-on: ubuntu-latest
      steps:
        - name: "Hello world"
          run: echo "Hello world"
      another-job:
        runs-on: ubuntu-latest
        steps:
          - name: "Update"
            run: echo "we've updated out code"
  
